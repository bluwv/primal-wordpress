{% extends "layouts/default.twig" %}

{% block content %}
    <?php if (have_posts()) : ?>
        <?php while (have_posts()) : the_post();
            the_content();
        endwhile; ?>
    <?php endif; ?>
{% endblock %}
